export default function rowCheck({num, column, board, currentColor}){
  let currentRowCheck = board[num]
    .map(row => {
      return row
    })

  console.log(currentRowCheck);

  // if(){
  //   return true
  // }
  //
  // return false
}
